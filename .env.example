DB_HOST=localhost
DB_USER=youruser        # Tên user bạn đã tạo ở bước tạo user (hoặc postgres nếu dùng user mặc định)
DB_PASSWORD=yourpassword  # Mật khẩu của user đó
DB_NAME=yourdb          # Tên database bạn đã tạo
DB_PORT=5432
JWT_SECRET=your_jwt_secret_string_here
PORT=8080
# RUN_SEEDER=false